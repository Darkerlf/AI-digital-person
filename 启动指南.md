# 景区导览 AI 数字人 — 启动指南

## 项目结构

```
.
├── backend/          # FastAPI 后端 API
├── admin-web/        # 管理后台前端 (Vue3 + Element Plus)
├── miniprogram/      # 游客端小程序 (uni-app)
└── 启动指南.md       # 本文件
```

---

## 环境要求

| 依赖 | 版本 | 用途 |
|------|------|------|
| Python | 3.11+ | 后端运行 |
| Node.js | 18+ | 前端构建 |
| MySQL | 5.7+ / 8.0 | 数据库 |
| npm | 9+ | 包管理 |

---

## 一、首次初始化（只需执行一次）

### 1. 创建 MySQL 数据库

```bash
mysql -u root -p123456 -e "CREATE DATABASE IF NOT EXISTS scenic_admin CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
```

### 2. 安装后端依赖

```bash
cd backend
pip install -r requirements.txt
```

### 3. 安装前端依赖

```bash
# 管理后台
cd admin-web
npm install

# 游客小程序
cd ../miniprogram
npm install
```

### 4. 初始化数据库表和管理员账号

```bash
cd backend
python init_db.py
```

执行成功后会创建：
- 所有数据表（scenic_area, knowledge_document, faq_item 等）
- 默认管理员账号：`admin` / `admin123`

### 5. 配置环境变量

编辑 `backend/.env` 文件：

```env
# 数据库连接（根据实际修改）
DATABASE_URL=mysql+pymysql://root:123456@127.0.0.1:3306/scenic_admin

# JWT 密钥（正式环境请修改）
JWT_SECRET_KEY=change-me

# 阿里云百炼 API KEY（用于 AI 对话）
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# AI 模型配置
LLM_MODEL=qwen3-max
EMBEDDING_MODEL=text-embedding-v3
```

---

## 二、日常启动

需要同时启动 **后端 API** 和 **前端**，建议开多个终端窗口。

### 终端 1：启动后端 API

```bash
cd backend
python -m uvicorn app.main:app --reload --port 8000
```

启动成功标志：
```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
```

验证：浏览器访问 http://localhost:8000/docs 查看 API 文档

### 终端 2：启动管理后台前端

```bash
cd admin-web
npx vite --port 3000
```

启动成功标志：
```
  VITE v8.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
```

验证：浏览器访问 http://localhost:3000

### 终端 3（可选）：启动游客小程序 H5 预览

```bash
cd miniprogram
npm run dev:h5
```

启动成功标志：
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
```

验证：浏览器访问 http://localhost:5173

---

## 三、端口分配

| 服务 | 默认端口 | 说明 |
|------|----------|------|
| 后端 API | 8000 | FastAPI 服务 |
| 管理后台 | 3000 | Vue3 管理后台 |
| 小程序 H5 | 5173 | uni-app H5 预览 |

> 如果端口被占用，Vite 会自动尝试下一个端口（如 5174、5175）。
> 管理后台建议使用 `--port 3000` 固定端口，避免与小程序冲突。

---

## 四、登录信息

### 管理后台

| 项目 | 值 |
|------|------|
| 地址 | http://localhost:3000 |
| 用户名 | `admin` |
| 密码 | `admin123` |
| 角色 | super_admin（超级管理员，拥有全部权限） |

### 三种角色权限

| 角色 | 可访问功能 |
|------|-----------|
| super_admin | 全部功能 |
| content_admin | 知识管理、景点管理、路线模板、FAQ、导入 |
| ops_admin | 运营概览、数据大屏、会话记录、反馈报告、操作日志 |

---

## 五、快速验证

### 1. 测试后端 API

```bash
# 健康检查
curl http://localhost:8000/api/health

# 测试 AI 对话（需要配置 DASHSCOPE_API_KEY）
cd backend
python test_chat_demo.py
```

### 2. 测试管理后台

1. 访问 http://localhost:3000
2. 使用 `admin` / `admin123` 登录
3. 左侧菜单可看到：运营概览、景区管理、景点管理、知识管理等

### 3. 测试小程序

1. 访问 http://localhost:5173
2. 点击底部「AI导游」标签
3. 输入问题（如「灵山大佛多高？」）测试对话

---

## 六、常见问题

### Q: 启动后端报错 `Unknown database 'scenic_admin'`

数据库未创建，执行：
```bash
mysql -u root -p123456 -e "CREATE DATABASE IF NOT EXISTS scenic_admin CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
python init_db.py
```

### Q: 启动后端报错 `Access denied for user 'root'`

密码不正确，修改 `backend/.env` 中的 `DATABASE_URL`：
```
DATABASE_URL=mysql+pymysql://root:你的密码@127.0.0.1:3306/scenic_admin
```

### Q: AI 对话无响应

1. 检查 `backend/.env` 中 `DASHSCOPE_API_KEY` 是否配置
2. 检查网络是否能访问 `dashscope.aliyuncs.com`
3. 查看后端终端是否有报错信息

### Q: 管理后台和小程序端口冲突

小程序默认使用 5173，管理后台建议用 3000：
```bash
# 管理后台
cd admin-web && npx vite --port 3000

# 小程序
cd miniprogram && npm run dev:h5
```

### Q: 前端页面空白

1. 确认后端已启动（http://localhost:8000/api/health 返回 `{"status":"ok"}`）
2. 确认前端没有报错（查看终端输出）
3. 清除浏览器缓存后重试

---

## 七、运行全部测试

```bash
cd backend
python -m pytest tests/ -v
```

当前测试数量：64 个，预期全部通过。
