# 景区导览 AI 数字人项目本地部署手册

本文档面向第一次接触代码部署的同学，目标是：别人从 GitHub 下载代码后，可以在自己的 Windows 电脑上把后端、后台管理端、微信小程序和 DH_live 数字人样片跑起来。

如果你只是想先看到页面，按“最小可运行模式”做即可；如果你还要体验 AI 问答、语音识别、路线规划、微信登录等功能，再继续配置对应的 Key。

## 0. 项目包含什么

项目目录大致如下：

```text
AI-digital-person/
  backend/                  FastAPI 后端接口服务
  admin-web/                Vue 后台管理端
  miniprogram/              uni-app 微信小程序端
  prototypes/dh-live-guide/ DH_live H5 数字人样片
  data/                     景区资料、导入数据
  docs/                     设计文档和开发说明
  uploads/                  本地上传目录
```

本地运行时通常需要同时打开 3 到 4 个终端窗口：

```text
窗口 1：后端 backend，端口 8000
窗口 2：后台管理端 admin-web，端口 5173
窗口 3：小程序构建 miniprogram
窗口 4：DH_live H5 静态服务，端口 58120，可选
```

## 1. 先安装这些软件

### 1.1 安装 Git

Git 用来下载代码。

下载地址：

```text
https://git-scm.com/download/win
```

安装时一路点“Next”即可。安装完成后，打开 PowerShell，输入：

```powershell
git --version
```

能看到版本号，例如 `git version 2.xx.x`，说明安装成功。

### 1.2 安装 Python

后端使用 Python。推荐 Python 3.11。

下载地址：

```text
https://www.python.org/downloads/windows/
```

安装时一定勾选：

```text
Add python.exe to PATH
```

安装完成后，在 PowerShell 输入：

```powershell
python --version
```

能看到 `Python 3.11.x` 或更高版本即可。

### 1.3 安装 Node.js

后台管理端和小程序都需要 Node.js。推荐 Node.js 20 LTS。

下载地址：

```text
https://nodejs.org/
```

安装完成后输入：

```powershell
node -v
npm -v
```

能看到版本号即可。

Windows PowerShell 下建议使用 `npm.cmd`，不要直接用 `npm`，这样可以避免脚本执行策略问题。

### 1.4 安装 MySQL

后端默认使用 MySQL。推荐 MySQL 8.0。

下载地址：

```text
https://dev.mysql.com/downloads/installer/
```

安装时记住你设置的 root 密码，后面要写到 `.env`。

安装完成后，如果系统能识别 `mysql` 命令，输入：

```powershell
mysql --version
```

能看到版本号即可。

如果提示找不到 `mysql`，也可以用 MySQL Workbench 创建数据库，或者把 MySQL 的 `bin` 目录加入系统环境变量。

### 1.5 安装微信开发者工具

小程序需要微信开发者工具。

下载地址：

```text
https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
```

安装完成后，用微信扫码登录。

## 2. 下载代码

选择一个你容易找到的位置，例如 `D:\Projects`。

打开 PowerShell：

```powershell
cd D:\
mkdir Projects
cd Projects
git clone https://github.com/Darkerlf/AI-digital-person.git
cd AI-digital-person
```

如果仓库默认分支不是开发分支，切到当前开发分支：

```powershell
git checkout feature/admin-backend-phase1
```

后续所有命令都假设你在项目根目录：

```text
D:\Projects\AI-digital-person
```

如果你的目录不同，把命令里的路径换成自己的即可。

## 3. 配置后端

### 3.1 创建数据库

打开 PowerShell，执行：

```powershell
mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS scenic_admin CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
```

执行后会让你输入 MySQL root 密码。输入时屏幕可能不显示字符，这是正常的。

如果你不会用命令，也可以打开 MySQL Workbench，新建一个数据库：

```text
数据库名：scenic_admin
字符集：utf8mb4
排序规则：utf8mb4_unicode_ci
```

### 3.2 创建 Python 虚拟环境

进入后端目录：

```powershell
cd D:\Projects\AI-digital-person\backend
python -m venv .venv
```

安装依赖：

```powershell
.\.venv\Scripts\python -m pip install -r requirements.txt
```

如果下载很慢，可以使用清华源：

```powershell
.\.venv\Scripts\python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 3.3 创建后端环境变量文件

项目根目录有 `.env.example` 模板。把它复制到 `backend\.env`：

```powershell
cd D:\Projects\AI-digital-person
Copy-Item .env.example backend\.env
notepad backend\.env
```

至少修改这几项：

```env
DATABASE_URL=mysql+pymysql://root:你的MySQL密码@127.0.0.1:3306/scenic_admin
JWT_SECRET_KEY=请换成一串随机字符
```

示例：

```env
DATABASE_URL=mysql+pymysql://root:123456@127.0.0.1:3306/scenic_admin
JWT_SECRET_KEY=local-dev-secret-please-change
```

可选配置如下。没有这些 Key，系统仍能启动，但相关功能会降级或不可用：

```env
DASHSCOPE_API_KEY=你的阿里云DashScopeKey
TENCENT_MAP_KEY=你的腾讯位置服务Key
WECHAT_APPID=你的小程序AppID
WECHAT_SECRET=你的小程序AppSecret
```

这些 Key 的作用：

```text
DASHSCOPE_API_KEY：AI 问答、语音合成、语音识别
TENCENT_MAP_KEY：景点坐标校准、步行路线规划
WECHAT_APPID / WECHAT_SECRET：微信小程序登录
```

重要：不要把真实 Key 提交到 GitHub。

### 3.4 初始化数据库表

在 `backend` 目录执行：

```powershell
cd D:\Projects\AI-digital-person\backend
.\.venv\Scripts\python scripts\init_db.py
```

这个脚本只负责创建数据库表，不会自动创建管理员账号。

### 3.5 创建后台管理员账号

继续在 `backend` 目录执行下面这段命令。它会创建一个默认管理员：

```text
账号：admin
密码：admin123
角色：超级管理员
```

PowerShell 命令如下，可以整段复制：

```powershell
@'
from app.core.database import SessionLocal
from app.core.security import hash_password
from app.models import load_all_models
from app.models.admin_user import AdminUser

load_all_models()
db = SessionLocal()
try:
    user = db.query(AdminUser).filter(AdminUser.username == "admin").one_or_none()
    if user is None:
        db.add(AdminUser(
            username="admin",
            password_hash=hash_password("admin123"),
            role="super_admin",
            status="active",
        ))
        db.commit()
        print("已创建管理员：admin / admin123")
    else:
        print("管理员 admin 已存在，无需重复创建")
finally:
    db.close()
'@ | .\.venv\Scripts\python -
```

正式使用时，请登录后台后立即修改密码。

### 3.6 启动后端

在 `backend` 目录执行：

```powershell
.\.venv\Scripts\python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

看到类似下面的内容说明启动成功：

```text
Uvicorn running on http://0.0.0.0:8000
```

不要关闭这个窗口。

打开浏览器访问：

```text
http://127.0.0.1:8000/api/health
```

如果看到：

```json
{"status":"ok"}
```

说明后端正常。

接口文档地址：

```text
http://127.0.0.1:8000/docs
```

## 4. 启动后台管理端

新开一个 PowerShell 窗口。

进入后台目录：

```powershell
cd D:\Projects\AI-digital-person\admin-web
```

安装依赖：

```powershell
npm.cmd install
```

如果下载很慢，可以切换 npm 镜像：

```powershell
npm.cmd config set registry https://registry.npmmirror.com
npm.cmd install
```

启动后台：

```powershell
npm.cmd run dev -- --host 0.0.0.0 --port 5173
```

浏览器访问：

```text
http://localhost:5173
```

登录：

```text
账号：admin
密码：admin123
```

如果页面能打开但登录失败，请检查：

```text
1. 后端窗口是否还在运行
2. http://127.0.0.1:8000/api/health 是否正常
3. 是否已经创建 admin 管理员
4. backend\.env 的 DATABASE_URL 是否连到了正确数据库
```

### 4.1 后台 API 地址配置

后台默认请求：

```text
http://127.0.0.1:8000/api
```

如果你想改成别的地址，可以在 `admin-web` 目录新建 `.env.local`：

```powershell
cd D:\Projects\AI-digital-person\admin-web
notepad .env.local
```

写入：

```env
VITE_API_BASE_URL=http://127.0.0.1:8000/api
```

改完后需要重启后台前端：

```powershell
Ctrl + C
npm.cmd run dev -- --host 0.0.0.0 --port 5173
```

## 5. 配置微信小程序

新开一个 PowerShell 窗口。

进入小程序目录：

```powershell
cd D:\Projects\AI-digital-person\miniprogram
```

安装依赖：

```powershell
npm.cmd install
```

### 5.1 配置小程序 AppID

仓库里不会提交真实 AppID，所以你拿到代码后需要自己填。

打开：

```text
miniprogram/project.config.json
```

把：

```json
"appid": "wx-your-appid-here"
```

改成你自己的 AppID，例如：

```json
"appid": "wx1234567890abcdef"
```

再打开：

```text
miniprogram/src/manifest.json
```

找到：

```json
"mp-weixin": {
  "appid": "wx-your-appid-here"
}
```

也改成自己的 AppID。

如果只是临时在微信开发者工具里预览，也可以使用测试号；但微信登录、真机调试、上传发布通常需要真实 AppID。

### 5.2 配置小程序本地接口地址

复制环境变量模板：

```powershell
cd D:\Projects\AI-digital-person\miniprogram
Copy-Item .env.example .env.local
notepad .env.local
```

微信开发者工具模拟器里运行，保持：

```env
VITE_API_BASE_URL=http://127.0.0.1:8000/api
```

如果你用手机真机预览，不能用 `127.0.0.1`。需要先查电脑局域网 IP：

```powershell
Get-NetIPAddress -AddressFamily IPv4 |
  Where-Object { $_.IPAddress -notlike "127.*" -and $_.PrefixOrigin -ne "WellKnown" } |
  Select-Object InterfaceAlias,IPAddress
```

假设查到电脑 IP 是：

```text
192.168.1.20
```

那么 `.env.local` 改成：

```env
VITE_API_BASE_URL=http://192.168.1.20:8000/api
```

注意：

```text
1. 后端必须用 --host 0.0.0.0 启动
2. 手机和电脑必须在同一个 Wi-Fi 下
3. Windows 防火墙不能拦截 8000 端口
```

### 5.3 构建微信小程序

在 `miniprogram` 目录执行：

```powershell
npm.cmd run build:mp-weixin
```

构建完成后，会生成：

```text
miniprogram/dist/build/mp-weixin
```

### 5.4 用微信开发者工具打开

打开微信开发者工具，选择：

```text
导入项目
```

项目目录选择：

```text
D:\Projects\AI-digital-person\miniprogram\dist\build\mp-weixin
```

不要直接导入：

```text
D:\Projects\AI-digital-person\miniprogram
```

因为微信开发者工具需要运行 uni-app 构建后的产物。

### 5.5 微信开发者工具本地调试设置

本地调试建议打开：

```text
详情 -> 本地设置 -> 不校验合法域名、web-view、TLS 版本以及 HTTPS 证书
```

如果页面一直用旧代码，执行：

```text
清缓存 -> 全部清除
```

然后重新编译。

### 5.6 小程序登录配置

如果要体验“我的页面”和游客登录，需要配置：

```env
WECHAT_APPID=你的小程序AppID
WECHAT_SECRET=你的小程序AppSecret
```

这两个配置写在：

```text
backend/.env
```

注意：改完 `backend/.env` 后，要重启后端。

## 6. 启动 DH_live 数字人样片

DH_live 是数字人 H5 页面，小程序 AI 导游页通过 `web-view` 打开它。

默认地址：

```text
http://localhost:58120/runtime/index.html
```

### 6.1 准备 DH_live runtime

项目里的：

```text
prototypes/dh-live-guide/runtime/
```

包含 DH_live Web 运行所需文件。由于 DH_live 原始素材、权重和演示角色可能涉及体积和授权问题，别人拿到代码后如果没有 `runtime`，需要自己准备。

如果 `runtime` 不存在，可以按 `prototypes/dh-live-guide/README.md` 的说明从 DH_live 官方 Web Demo 文件生成：

```powershell
cd D:\Projects\AI-digital-person\prototypes\dh-live-guide
$sourceRepo = Join-Path $env:TEMP 'dh-live-source'
if (-not (Test-Path (Join-Path $sourceRepo 'web_demo\static\js\DHLiveMini.js'))) {
    git clone --depth 1 https://github.com/kleinlee/DH_live.git $sourceRepo
}
.\scripts\stage-dh-live-runtime.ps1 -SourceStaticDir (Join-Path $sourceRepo 'web_demo\static')
```

如果你已经有授权后的 DH_live runtime 文件，直接放到：

```text
prototypes/dh-live-guide/runtime/
```

目录里即可。

至少需要这些文件：

```text
runtime/index.html
runtime/DHLiveMini.wasm
runtime/js/DHLiveMini.js
runtime/js/MiniMateLoader.js
runtime/js/MiniLive2.js
runtime/js/pako.min.js
runtime/assets/01.mp4
runtime/assets/combined_data.json.gz
runtime/background/bg.mp4
```

### 6.2 启动 DH_live 静态服务

新开 PowerShell：

```powershell
cd D:\Projects\AI-digital-person\prototypes\dh-live-guide
python -m http.server 58120 --bind 0.0.0.0
```

浏览器访问：

```text
http://localhost:58120/runtime/index.html
```

如果能看到数字人页面，说明 DH_live 本地服务正常。

### 6.3 小程序里配置 DH_live 地址

微信开发者工具模拟器里，默认就是：

```text
http://localhost:58120/runtime/index.html
```

如果你要改地址，可以在：

```text
miniprogram/.env.local
```

写入：

```env
VITE_DH_LIVE_GUIDE_URL=http://localhost:58120/runtime/index.html
```

如果是真机或正式上线，`web-view` 不能使用普通 HTTP 局域网地址，必须使用微信公众平台配置过的 HTTPS 业务域名，例如：

```env
VITE_DH_LIVE_GUIDE_URL=https://guide.example.com/runtime/index.html
```

个人小程序通常无法配置 `web-view` 业务域名，所以 DH_live H5 更适合开发演示或企业小程序上线。

## 7. AI、语音、地图能力配置

### 7.1 DashScope

用于：

```text
AI 问答
语音合成 TTS
语音识别 ASR
```

配置位置：

```text
backend/.env
```

示例：

```env
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxx
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=qwen-plus
TTS_MODEL=cosyvoice-v3-flash
TTS_VOICE=loongbella_v3
```

没有 `DASHSCOPE_API_KEY` 时：

```text
后端可以启动
部分问答可能变成兜底回答
语音合成和语音识别可能不可用
```

### 7.2 腾讯位置服务

用于：

```text
景点坐标校准
地图点位
步行路线规划
```

配置位置：

```text
backend/.env
```

示例：

```env
TENCENT_MAP_KEY=你的腾讯位置服务Key
```

腾讯位置服务控制台需要开启 WebService API。

没有这个 Key 时：

```text
地图仍可展示已有数据库坐标或预设坐标
步行路线可能使用直线兜底
坐标自动校准不可用
```

### 7.3 阿里云 OSS

用于：

```text
游客头像上传
TTS 音频文件公开访问
```

配置位置：

```text
backend/.env
```

示例：

```env
STORAGE_BACKEND=auto
ALIYUN_OSS_ACCESS_KEY_ID=你的AccessKeyId
ALIYUN_OSS_ACCESS_KEY_SECRET=你的AccessKeySecret
ALIYUN_OSS_BUCKET=你的Bucket
ALIYUN_OSS_ENDPOINT=oss-cn-beijing.aliyuncs.com
ALIYUN_OSS_PUBLIC_BASE_URL=
ALIYUN_OSS_KEY_PREFIX=scenic-guide
```

本地只体验后台和小程序基本页面，可以先不配置 OSS。

## 8. 导入或录入景区数据

第一次启动后，数据库是空的。你可以通过后台管理端录入：

```text
景区
景点
服务点
FAQ
知识库文档
路线模板
数字人配置
```

后台地址：

```text
http://localhost:5173
```

登录后从左侧菜单进入对应模块。

项目里的 `data/` 目录保存了一些源资料，可以通过后台导入功能上传，也可以按项目已有导入脚本进行处理。

可选脚本：

```powershell
cd D:\Projects\AI-digital-person\backend
.\.venv\Scripts\python scripts\seed_faq_from_project_docs.py
.\.venv\Scripts\python scripts\sync_knowledge_base_from_data.py
```

如果脚本执行失败，先不用慌，可以先在后台页面手动录入少量测试数据。

## 9. 启动顺序建议

最稳的启动顺序：

```text
1. 启动 MySQL
2. 启动后端 backend
3. 启动后台 admin-web
4. 启动 DH_live 静态服务
5. 构建小程序
6. 用微信开发者工具打开小程序
```

推荐每个服务单独开一个 PowerShell 窗口。

## 10. 常用命令汇总

### 后端

```powershell
cd D:\Projects\AI-digital-person\backend
.\.venv\Scripts\python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### 后台管理端

```powershell
cd D:\Projects\AI-digital-person\admin-web
npm.cmd run dev -- --host 0.0.0.0 --port 5173
```

### 小程序构建

```powershell
cd D:\Projects\AI-digital-person\miniprogram
npm.cmd run build:mp-weixin
```

### DH_live

```powershell
cd D:\Projects\AI-digital-person\prototypes\dh-live-guide
python -m http.server 58120 --bind 0.0.0.0
```

## 11. 测试命令

不是必须，但建议开发者跑一下。

### 后端测试

```powershell
cd D:\Projects\AI-digital-person\backend
.\.venv\Scripts\python -m pytest tests -q
```

### 后台测试和构建

```powershell
cd D:\Projects\AI-digital-person\admin-web
npm.cmd run test
npm.cmd run build
```

### 小程序测试和构建

```powershell
cd D:\Projects\AI-digital-person\miniprogram
Get-ChildItem tests -Filter *.mjs | ForEach-Object { node $_.FullName }
npm.cmd run build:mp-weixin
```

### DH_live 测试

```powershell
cd D:\Projects\AI-digital-person\prototypes\dh-live-guide
npm.cmd test
```

## 12. 常见问题

### 12.1 后端启动报 MySQL 连接失败

常见原因：

```text
1. MySQL 没启动
2. backend/.env 里的密码不对
3. scenic_admin 数据库没创建
4. DATABASE_URL 写错
```

检查：

```powershell
mysql -u root -p -e "SHOW DATABASES;"
```

确认能看到：

```text
scenic_admin
```

### 12.2 后台登录失败

检查：

```text
1. 后端是否启动
2. 是否已经执行 scripts/init_db.py
3. 是否已经创建 admin/admin123
4. 后台请求地址是否是 http://127.0.0.1:8000/api
```

可以重新执行创建管理员命令。已存在时不会重复创建。

### 12.3 PowerShell 提示 npm 不能运行

使用：

```powershell
npm.cmd install
npm.cmd run dev
```

不要直接用：

```powershell
npm install
npm run dev
```

### 12.4 小程序请求 127.0.0.1 失败

如果是微信开发者工具模拟器，`127.0.0.1` 通常可以用。

如果是真机，`127.0.0.1` 指的是手机自己，不是电脑。需要改成电脑局域网 IP：

```env
VITE_API_BASE_URL=http://你的电脑IP:8000/api
```

并确保后端使用：

```powershell
--host 0.0.0.0
```

### 12.5 小程序登录失败

检查：

```text
1. miniprogram/project.config.json 是否填了真实 AppID
2. miniprogram/src/manifest.json 是否填了同一个 AppID
3. backend/.env 是否配置 WECHAT_APPID 和 WECHAT_SECRET
4. 后端是否重启
5. 微信开发者工具是否清缓存后重新编译
```

### 12.6 AI 导游页打不开

检查 DH_live 服务：

```text
http://localhost:58120/runtime/index.html
```

如果浏览器都打不开，说明 DH_live 静态服务没启动，或者 runtime 文件不存在。

启动：

```powershell
cd D:\Projects\AI-digital-person\prototypes\dh-live-guide
python -m http.server 58120 --bind 0.0.0.0
```

### 12.7 AI 导游语音输入提示麦克风权限

需要同时满足：

```text
1. 小程序允许 scope.record 麦克风权限
2. Windows 系统允许微信开发者工具访问麦克风
3. DH_live H5 页面在 localhost 或 HTTPS 下运行
```

如果 H5 WebView 仍然拒绝麦克风，当前项目已提供原生录音兜底页。按住说话后，会识别文本并自动回到 AI 导游页继续提问。

### 12.8 微信开发者工具一直显示旧页面

执行：

```text
清缓存 -> 全部清除
```

然后重新运行：

```powershell
cd D:\Projects\AI-digital-person\miniprogram
npm.cmd run build:mp-weixin
```

再重新导入或刷新：

```text
miniprogram/dist/build/mp-weixin
```

### 12.9 端口被占用

查看端口占用：

```powershell
Get-NetTCPConnection -LocalPort 8000 -ErrorAction SilentlyContinue |
  Select-Object LocalAddress,LocalPort,State,OwningProcess
```

结束进程：

```powershell
Stop-Process -Id 进程ID -Force
```

后台常用端口：

```text
8000：后端
5173：后台管理端
58120：DH_live 静态服务
```

如果不想结束进程，也可以换端口。

## 13. 正式上线前必须知道

本手册主要讲本地运行。正式上线还需要额外处理：

```text
1. 后端部署到服务器，并使用 HTTPS 域名
2. 微信公众平台配置 request 合法域名
3. 如果使用 web-view，配置业务域名
4. 数据库不能使用本地 MySQL
5. JWT_SECRET_KEY 必须换成强随机密钥
6. 所有真实 Key 只能放服务器环境变量，不能提交到 GitHub
7. 小程序上传发布前必须使用真实 AppID
8. OSS、DashScope、腾讯地图等云服务需要确认计费和额度
```

个人小程序通常不能配置 `web-view` 业务域名，因此 DH_live H5 数字人方案更适合企业小程序或本地演示。如果要个人小程序正式上线，需要评估原生小程序数字人方案或其他合规承载方式。

## 14. 最小可运行检查清单

按下面清单逐项确认：

```text
[ ] Git 已安装，能执行 git --version
[ ] Python 已安装，能执行 python --version
[ ] Node.js 已安装，能执行 node -v 和 npm -v
[ ] MySQL 已安装并创建 scenic_admin 数据库
[ ] backend/.env 已配置 DATABASE_URL 和 JWT_SECRET_KEY
[ ] 后端依赖已安装
[ ] 已执行 scripts/init_db.py
[ ] 已创建 admin/admin123
[ ] 后端 http://127.0.0.1:8000/api/health 返回 ok
[ ] admin-web 依赖已安装
[ ] http://localhost:5173 能打开后台登录页
[ ] 小程序 AppID 已按需替换
[ ] miniprogram/.env.local 已配置 VITE_API_BASE_URL
[ ] npm.cmd run build:mp-weixin 成功
[ ] 微信开发者工具导入 dist/build/mp-weixin
[ ] 如需 AI 导游，DH_live 的 runtime 已准备并启动 58120 服务
```

全部勾上后，本地部署就完成了。
